{
    "name": "vira-test",
    "version": "1.0.0",
    "description": "",
    "sources": "src/static/index.html",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1 ",
        "start": "rm -rf .parcel-cache && parcel build assets/* && parcel ./src/static/index.html --open",
        "build": "rm -rf .parcel-cache && parcel build ./src/static/index.html --no-source-maps --no-cache --public-url ."
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@parcel/transformer-sass": "^2.11.0",
        "parcel": "^2.11.0",
        "parcel-bundler": "^1.10.3"
    },
    "dependencies": {
        "core-js": "^3.36.0",
        "jquery": "^3.7.1",
        "jquery-validation": "^1.20.0"
    }
}
